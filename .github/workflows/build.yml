name: Build page
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.0
      with:
        username: 'cyaninfinite'
        reponame: 'uclist'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged
